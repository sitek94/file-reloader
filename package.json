{
  "name": "files-reloader",
  "description": "Super simple WebSocket based file reloader",
  "scripts": {
    "start": "node server/index.mjs"
  },
  "devDependencies": {
    "@types/ws": "^8.2.2",
    "ws": "^8.4.2"
  }
}
